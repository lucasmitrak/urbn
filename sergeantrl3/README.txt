Sergeant RL-3 is named after the Mister Gutsy robots from Fallout.

Sergeant RL-3 is the twitter scraper.
It grabs the data from twitter catfish and the 2nd degree data.
It puts it into a local database.
It then sends the data via ssh to the main server.

This is to be run on a node.
This should be run on a node where the catfish is managed.
You may write the data through a ssh tunnel if desired.

8.31.2015
grey

TODO: gather all tweets and followers by overcoming timeout through time or another way
